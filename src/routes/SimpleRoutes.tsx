// project imports
import SimpleLayout from 'layout/SimpleLayout';

// ==============================|| SIMPLE ROUTING ||============================== //

const SimpleRoutes = {
  path: '/',
  element: <SimpleLayout />,
  children: []
};

export default SimpleRoutes;
